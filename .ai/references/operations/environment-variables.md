# Operations Reference: Environment

Important environment controls include offline mode, XDG roots, cloud/profile configuration, test isolation, and update-check controls. Use the generated source index to locate reads before changing names. Never include actual environment values in `.ai` output.
