# Operations Reference: Packaging

AppImage build files are under [`packaging/`](../../../packaging/). Packaging must include Python modules/assets needed by `main.py`, preserve XDG behavior, and not package local credentials or developer cache data.
