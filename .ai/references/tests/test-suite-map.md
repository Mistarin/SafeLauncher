# Test Reference: Suite Map

Focused tests are under [`tests/`](../../../tests/). Request/resource changes map to `test_request_contracts.py`, `test_request_manager.py`, `test_resource_cache.py`, and `test_resource_binding.py`. Cloud queue, profile, Steam, performance, global-hotkey, network-policy, security/thread, and historical broad coverage each have dedicated paths.

The generated test index records test classes/functions and command references.
