# Workflow: Game Exit and Playtime

Playtime is checkpointed during a running session and finalized after process exit. The local database remains usable if the network is unavailable. Later private sync merges session and summary metadata across devices without making a remote response necessary for local display.
